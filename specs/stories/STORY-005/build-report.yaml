story_id: STORY-005
runtime:
  language: node
  language_version: ">=20.0.0"
  package_manager: npm
  build_command: "npm install"
  build_output_dir: "src/"
  has_dockerfile: false
  has_migrations: false
  exposed_ports: []
files_modified:
  - src/tools/dashboard.js
  - package.json
  - package-lock.json
  - specs/stories/STORY-005/story-spec.md
  - specs/architecture/ux.md
commits: []
warnings:
  - "pattern:html-generation specifies template module at src/lib/dashboard/template.js; implementation keeps all HTML generation in src/tools/dashboard.js to avoid unnecessary indirection — no functional impact on criteria"
  - "bootstrap, bootstrap-icons, and chart.js installed as runtime dependencies (npm install bootstrap@5.3.3 bootstrap-icons@1.11.3 chart.js@4.4.3)"
  - "Bootstrap Icons CSS references woff2 font — patched at generation time: font file read from node_modules and embedded as base64 data URI so dashboard is fully offline-capable"
  - "Enhancement 4: Chart.js datalabels plugin not available in inlined build; point value labels delivered via tooltip (hover) and y-axis tick formatter instead of on-canvas labels — no external plugin dependency added"
  - "Enhancement 4: month field in contract is an abbreviation (Jan/Feb/…); YYYYMM comparison uses year key + MONTH_ABBR_TO_NUM lookup to filter months beyond reporting_month"
  - "Enhancement (gap.md 2026-06-29): Mermaid.js loaded from CDN (cdn.jsdelivr.net/npm/mermaid) rather than inlined — the rest of the dashboard remains fully offline-capable; diagram section is skipped when enterprise_architecture_diagram is empty/absent (graceful degrade per customer)"
  - "Enhancement (gap.md 2026-06-29): Mermaid CDN script tag and mermaid.initialize() are only emitted when at least one customer has a non-empty enterprise_architecture_diagram — zero cost for portfolios that have not run --analyze with STORY-003"
  - "Enhancement (gap.md 2026-06-29): customerAnnualBudget() and l1AnnualBudget() removed; Full-Year ACV/Budget/Consumed now derived from L3 projected_annual_* fields via l3AnnualTotals() / l1AnnualTotals() / customerAnnualTotals() helpers; verified Consumer Products Full-Year Budget (12.6M) is ~8.8x Full-Year ACV (1.4M) — bug previously causing equality is fixed; variance field reads updated from acv_gap/budget_gap/budget_attainment to ytd_acv_gap/ytd_budget_gap/ytd_budget_attainment"
  - "Enhancement (gap.md 2026-06-30): YTD Att% and Proj Att% tiles appended to industry header strip in buildIndustryView(); YTD att% uses C_PCT (#1d4ed8), Proj att% uses #8ecae6 (matching the projected donut center text color at line 908); both values show '—' when budget is zero"
  - "Enhancement registration 2026-06-30: two-tone color scheme (dark nav #0f172a, dark-teal tab bars #0e7490, light-teal subheader #e0f2fe) already implemented in src/tools/dashboard.js; Criterion 8 and ux:visual-system/ux:component-conventions/ux:screen-template updated to match; gap.md deleted after merge"
overall_status: pass
